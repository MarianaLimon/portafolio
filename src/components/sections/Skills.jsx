import SectionCard from "../ui/SectionCard";
import SectionTitle from "../ui/SectionTitle";

export default function Skills() {
  return (
    <SectionCard>
      <SectionTitle>Skills</SectionTitle>
      <p>Contenido de Skills...</p>
    </SectionCard>
  );
}